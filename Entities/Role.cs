namespace WebsiteQLNhaTro.Entities
{
    /// <summary>
    /// Role enum: defines user roles in the system.
    /// </summary>
    public enum Role
    {
        Admin = 1,
        Owner = 2,
        Tenant = 3
    }
}
